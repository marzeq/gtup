# g(e)tup

a simple cli wake-on-lan by magic packet utility

wake up a device by mac address. additionally, save commonly used clients and reference them by their name

## usage

```bash
gtup [mac addr/saved name]
gtup --save [name] [mac addr]
gtup --edit [name] [new mac addr]
gtup --delete [name]
```
