# g(e)tup

a simple cli wake-on-lan by magic packet utility

wake up a device by an mac address or local ip address/hostname that resolves to a mac. additionally, save commonly used clients and reference them by their name

## usage

```bash
gtup [mac addr/local ip/hostname/saved name]
gtup --save [name] [mac addr]
```
